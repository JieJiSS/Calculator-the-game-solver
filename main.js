/* Script by JieJiSS, in France & Italy, 2017. Presented with ❤
 * @require environment: node.js > 7.x.x (optional `--harmony-async-await` argument)
 * @usage run `main()` and follow the instruction.
 * @dependency npm package `promptly`
 * @license Apache license 2.0
 * */
"use strict";

const { main } = require("./solver");

main();
